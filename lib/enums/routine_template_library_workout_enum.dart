enum RoutineTemplateLibraryWorkoutEnum {
  ppl("Push Pull LEGS"), upperLower("UPPER LOWER"), broSplit("BRO SPLIT"), fullBody("FULL BODY");

  const RoutineTemplateLibraryWorkoutEnum(this.name);

  final String name;
}